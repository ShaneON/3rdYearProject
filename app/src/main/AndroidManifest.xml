<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sca.shane.soccerapp">



    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:debuggable="true">

        <activity android:name=".PlayScreen"
            android:screenOrientation="portrait"
            android:label="Play Screen"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".Register"
            android:label="Register screen"
            android:theme="@style/AppTheme" />

        <activity android:name=".TeamName"
            android:label="Team Name"
            android:theme="@style/AppTheme" />

        <activity android:name=".AddPlayers"
            android:label="Add Players"
            android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".MainMenu"
            android:label="Soccer Coach Assistant"
            android:theme="@style/AppTheme" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".PickOpp"
            android:label="Pick Opposition"
            android:theme="@style/AppTheme" />

        <activity android:name=".PickStarting11"
            android:label="Select Starting 11"
            android:theme="@style/AppTheme" />


    </application>

</manifest>
